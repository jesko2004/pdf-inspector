# 项目首页与交付信息审核稿

日期：2026-09-11。状态：**用户已于 2026-09-11 回复“可提交”，本轮中文首页与简介、主题标签方案获准交付。版本发布文案仍为备选，不创建版本标签或安装包。**

本次交付范围是项目首页、仓库简介和标签说明。首页正文、标题、示例注释均使用中文；项目名称、技术名称、接口标识、参数和命令保留原样。GitHub 自身的界面文字由账号语言与平台控制，不属于仓库文案。

2026-09-11 已通过 GitHub 接口核实，验收收尾 PR #12 已合并；本轮是其后的介绍文案更新，不修改既有功能或验收结论。

## 一、项目首页

完整稿见 [中文项目首页](../README.md)。

首页介绍：

> 面向文档处理与智能体应用的 PDF 分类、文字提取和结构化转换工具。
>
> 使用 Rust 解析 PDF，将可提取的文字、表格和页面结构转换为 Markdown 或 JSON；提供 Python、Node.js 和浏览器 WebAssembly 绑定。可选本地后端支持扫描页文字识别、业务字段提取、知识库检索、来源引用及备份恢复。

首页按使用场景、核心能力、快速开始、本地后端、验收边界、处理方式和开发文档排列。已纠正原文的两项过时描述：表格检测采用三种策略；当前默认扫描配置为 `Sample(8)`。移除没有本轮测量依据的毫秒级耗时宣传，并明确核心解析与可选 OCR 的区别。

## 二、仓库简介

建议填写到 GitHub 仓库右侧“关于”的简介：

> 基于 Rust 的 PDF 分类、文字提取与 Markdown 转换工具，支持本地文字识别、业务字段提取及带来源引用的知识库检索。

项目名称保留 `pdf-inspector`。项目官网地址暂不新增；现有接口文档与使用指南由首页链接提供。

## 三、首页主题标签

本次采用首页主题标签方案；下一节保留版本发布页的中文备选文案，不自动创建软件版本。

首页可见的中文关键词为：

> PDF 解析 · 文字提取 · 文档分类 · 表格识别 · 本地文字识别 · 知识库检索

GitHub 主题标识需使用小写字母、数字和连字符，因此不直接填写中文。这是 [GitHub 主题规则](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/classifying-your-repository-with-topics) 的限制。主题本身保留通用标识，含义和首页介绍使用中文。

| 拟使用的主题标识 | 中文含义 |
|---|---|
| `pdf` | PDF 文档处理 |
| `pdf-parser` | PDF 解析 |
| `text-extraction` | 文字提取 |
| `markdown` | Markdown 内容转换 |
| `table-extraction` | 表格提取 |
| `ocr` | 可选本地文字识别 |
| `rust` | Rust 核心实现 |
| `python` | Python 接口与后端 |
| `webassembly` | 浏览器本地解析 |
| `rag` | 带来源引用的知识库检索与问答流程 |

以上为本次获准交付的主题配置；标签不表示全部功能已完成生产环境验收。

## 四、版本标签与发布页备选稿

如果“标签页”指版本标签或发布页，可采用以下中文介绍。此处不创建 Git 标签、不分配新版本号，也不上传未经构建验收的安装包；Rust、Python 和 Node.js 配置当前具有各自的版本号，不能直接假设为同一个发布版本。

**发布页标题：**

> 离线本地验收收尾版：完善 PDF 提取、文字识别与备份恢复

**发布说明：**

本次完成 PDF 提取与离线本地后端的限定范围验收。修复多栏阅读顺序、侧栏标题插入、双语说明误判、复选框与单位提取问题；为符合条件的图片封面补充本地文字识别，并保留原生文字。

已知发票模板的编号、四列表格和金额处理得到完善，源字段冲突继续保留人工复核状态。备份恢复会重新绑定任务文件路径，在原目录不可用时，恢复后的结果、检索及重新入库仍可使用。

- Rust 测试 893 项通过，格式与严格静态检查通过。
- 后端测试 95 项通过，1 项按本地 SQLite 范围跳过；Python 绑定与脚本测试分别为 64 项和 17 项通过。
- 完成 16 份、86 页 PDF 的既定内容覆盖检查，以及 22 项完整接口与独立恢复检查。
- 外部 `pdf-evals` 未运行，已获本次收尾单次豁免；不宣称等价覆盖。

当前通过范围为 SQLite 单实例、本地 RapidOCR 和 `hash` / `extractive` 工程流程。通用图像文字补全、完整手写与旋转印章识别、真实模型回答质量及生产服务等级不在本轮保证范围内。

本段是基于已完成验收编写的发布说明审核稿，不表示已创建版本标签或发布安装包。

## 五、本轮中文文案变更的提交说明

**建议标题：**

> 文档：完善全中文项目首页、简介和标签说明

**建议描述：**

将项目首页说明统一为中文，补充使用场景、快速开始、本地后端配置、验收范围和文档入口。按当前代码修正三种表格检测策略及默认抽样配置，保留命令、参数和接口标识的原始写法。准备中文仓库简介、主题标签释义和发布页备选文案。

本次仅修改文档，未改变功能、依赖或模型。文案不扩大已有验收结论，外部回归继续保留“未运行、本次已获豁免”。

## 六、审核后交付顺序

1. 确认首页表述、仓库简介，以及“标签页”需要采用的方案。
2. 在最新主分支上整理本轮文档差异，按项目要求完成提交检查后创建新的文档 PR；不向已合并的 PR #12 追加提交。
3. 审核同意后更新仓库简介和主题；版本标签与安装包发布需明确版本及产物范围后另行执行。
4. 提交实际页面或 PR 链接，核对中文展示、文档导航和能力边界。

本次审核已通过，继续执行文档提交、PR 检查与合并、仓库简介和主题更新，最后核对默认分支首页。软件版本标签与安装包发布不在本次操作范围。
