# 最终收尾报告

日期：2026-09-10。**离线本地版本在约定范围内基本验收通过，保留已知限制。** 功能修复、内容回归、实际完整流程及独立恢复已完成；外部回归与 GitHub 交付状态仍须以下方实际记录为准。

## 交付内容

多栏正文、侧栏标题、双语说明及复选框问题已修复；本地 RapidOCR 对符合条件的图片封面补充文字并保留原生内容；重复绘制的发票标题恢复正常；中文换行不再向已覆盖的文字边界插入词内空格。Freon 数值单位、已知发票编号和四列表格、USD 金额归一化、Uvicorn 日志以及备份恢复路径问题也已关闭。

发票源文件的 BSB 候选冲突继续保持 `needs_review`，没有为取得 ready 而消除真实冲突。

## 验收证据

| 检查 | 结果 |
|---|---|
| 格式、严格 Clippy | 通过 |
| Rust | 893 通过 |
| 后端 | 95 通过；pgvector 真实连接 1 项因未选用而跳过 |
| Python 绑定、脚本 | 64、17 通过 |
| 真实本地覆盖 | 16 份、86 页；23 页整页 OCR、1 页封面补充；37 项既有保护断言和 45 项后续检查通过 |
| OCR 稳定性 | 8 任务通过，含 3 工作线程处理 6 个并发任务；队列清空，无异常失败 |
| 最终完整流程及恢复 | 22 项检查通过；封面文字入库、检索、问答引用、冲突状态、备份校验、原路径不可用时读取与检索、恢复后重新入库均通过 |

最终流程在独立回环服务执行。备份包含 9 个数据文件；恢复到另一目录后，源验收目录保持不可用，任务上传及结果路径均已绑定恢复目录。恢复后的结果、配置、检索片段和引用与恢复前一致。测试服务已关闭，未操作正式业务数据。

验收原生扩展 SHA-256：`df247301d98aa6f1593b1b4bcaaebfc20d3d296ba8caafb1b9c4498aedeca294`。源码及构建产物与已通过的修复候选一致；本次没有重跑未受影响的整套自动化，也没有将历史性能记录当作新测量。流程原始证据保留在本机 `test_output/acceptance-closeout/flow-20260910-214740/`。

## 已知限制

本轮使用 SQLite 单实例、本地 RapidOCR、hash/extractive，验证工程流程和引用协议，不证明真实模型回答质量或生产 SLA。封面补充只覆盖符合保守几何条件的页面；Rust CLI 本身不执行 OCR，任意图像文字补全、完整手写和旋转印章识别仍不作保证。其他排版问题仅在不影响关键值、单位、语义或来源关系时暂缓。

BM25、多租户、分布式队列、对象存储与真实模型接入不作为本轮交付前置条件。

## 提交与 GitHub 状态

外部 pdf-evals 未执行；2026-09-10，项目负责人明确回复“选择路径Ｂ收尾”，批准仅针对本次收尾提交豁免外部 pdf-evals。套件仍未运行，不宣称等价覆盖；长期规则不变。2026-09-10 查询项目作者的 `jesko2004/pdf-evals` 与原始上游的 `firecrawl/pdf-evals` 均返回 HTTP 404；公开仓库名称搜索未找到兼容套件。404 也可能代表私有或当前身份不可访问，不能据此断言仓库不存在。[上游 AGENTS.md](https://github.com/firecrawl/pdf-inspector/blob/main/AGENTS.md) 仍引用该外部套件，但未给出可获取的兼容来源。本次单次例外已明确批准，无需再提供离线副本或重复批准。

35 个待提交文件、复现材料及 [GitHub 标题、描述与中文总结](github-closeout-draft.md) 已整理。本地基线为 `42c8faa874820b77a9a9c73efa87e4e9ae382598`；实时核实远程 main 为 `faca8a36742d6fa516529df81dcb99ac70947aae`，仅增加已合并 PR #11 的提交，文件差异为空。现有功能验证不因该合并失效，正式发布前仍需刷新目标分支。

本文记录收尾提交的验收依据；本地提交及发布动作按路径 B 执行。发布检查发现 GitHub CLI 登录凭据无效（401），连接器返回目标仓库 `push: false`，Git HTTPS 刷新曾连接超时。尚无 PR 链接；实际提交号及发布结果另记本机交付记录，不把准备完成当成已发布。

复现方法见 [离线验收复现说明](acceptance-reproduction.md)，修复细节见 [后续修复报告](acceptance-deferred-fixes.md)。原始业务 PDF、独立测试数据库、访问密钥及无关简历文件不纳入交付。

外部回归的作用、剩余风险及本次批准记录见 [完整解决方案](external-regression-resolution.md)。路径 B 已批准；长期规则变更未授权，AGENTS.md 保持不变。
