# 第一阶段修复验收报告

日期：2026-09-10。结论：**第一阶段 Q-01、Q-02、Q-03 已修复并完成定向验收，可以关闭。** 本次没有执行剩余阶段的范围确认、外部回归、Git 提交或发布，不能据此宣布全部正式收尾完成。

## 修复与关闭证据

| 问题 | 修复 | 已验证结果 |
|---|---|---|
| Q-01 多栏串读 | 用低占用区的实际空隙校准栏间边界；允许短段落与长栏独立阅读；识别孤立的多栏粗体短新闻区；不把含页码的新闻引用当作数值表格证据 | 杂志第 1 页八条新闻完整且按四栏排列；第 11 页左栏结束后再读右栏，没有跨栏拼句 |
| Q-02 侧栏标题插入 | 对长正文旁字号显著较大的短标题区保持独立顺序 | 第 1 页 per gallon of water 连续，Emergency Supply List 位于正文之后；第 2 页与前轮输出相同 |
| Q-03 双语表单误判、复选框失真 | 矩形网格不得用占主导的长段落充当单元格；Wingdings2 的 F0A3 按源字体恢复为空方框，不替换普通货币 £ | 第 1 页说明、链接及强调片段回到原句位置；第 48 页十个复选框为 □，没有错误 £ |
| 复测发现的表单值顺序退化 | AcroForm 值追加在内容流之后，改用控件坐标排序，保持标签和值关联 | 第 11 页“總計 Total total7_1a: 0”位于后续 (b) 段之前；新增回归覆盖这一关系 |

上述算法依据几何、文本形态和字体语义，不依据文件名、文件哈希或源样本的特定新闻句子绕过处理。六项新增合成数据单元测试可以在没有本机业务 PDF 的情况下运行；已有数值表格反例测试增强为包含 Page 数字的文本引用，避免将引用当数值。

## 最终候选验证

- Rust：890 通过（741 库测试、1 二进制、146 集成、2 文档）；格式及严格 Clippy 通过；原有快照没有因本阶段修改而更新。
- 后端：90 项中 89 通过、1 跳过；pgvector 未选用，真实本地 OCR 测试通过。
- Python 绑定：64 通过；脚本：17 通过。
- 真实本地服务：16 份 PDF、86 页、23 页 OCR；15 ready、1 needs_review，后者为发票源 BSB 冲突的预期复核行为。
- 发票编号正反例、四列表头、日期金额和 CSV 均通过；Freon 单位和输出保留；混合样本仍仅第 2 页 OCR，跨页来源检查通过。
- 最终两组服务日志没有 Traceback、日志格式化错误或非预期 HTTP 5xx；验收服务已退出。并发/内存不在本阶段重测范围，前轮数字仍仅代表前轮候选。

与前轮候选逐页比较，变化仅在三个目标 PDF，共 20 页：杂志 2 页、清单 1 页、双语表单 17 页。其他 13 份 PDF 的逐页 Markdown 完全相同。

双语表单 17 页中，10 页仅复选框映射变化，1 页为段落恢复，6 页因表单字段按位置排列而变化。对第 1、7、10、11、21、24、27、48 页已对照源页核查，另外十个仅符号变化的页面检查了差异一致性。忽略 Markdown 格式并将旧错误符号对应到正确方框后，变更页的非格式字符均保留。字符保留检查只作为辅助，不能单独证明阅读顺序正确。

## 版本与证据

- HEAD：`42c8faa874820b77a9a9c73efa87e4e9ae382598`；仍为工作区未提交候选。
- 最终原生扩展 SHA256：`2da524447b940fa7b16aa3de382fed489dece01d22e5287d4429ae86bdc83bd9`。
- 本阶段源码变更：src/extractor/layout.rs、src/extractor/fonts.rs、src/tables/detect_lines.rs、src/tables/detect_rects.rs；保留前轮已关闭修复。
- 汇总及检查：test_output/acceptance-stage1/summary.json、verification.json、changed-page-review.json、各测试日志。verify_results.py 可对保存的真实服务结果重新执行断言。
- 最终覆盖结果：`test_output/acceptance-next/coverage-20260910-200234/`；包含逐页结果、Markdown、首尾页及跨页检索证据。
- 最终发票复测：`test_output/acceptance-next/invoice-20260910-200546/`。
- 之前的报告已归档到本阶段目录下 before-stage1-*，原始测试数据未覆盖；test_output 仍受 Git 忽略，原始证据只在本机。

## 保留事项

Q-04 封面图像文字补全没有实现；它是否按方案暂缓及交付范围如何表述，属于下一阶段。手写/印章、装饰标题、任意复杂版式和真实模型语义质量也没有因这三个样本通过而获得通用准确率保证。

后续从第二阶段继续：冻结暂缓范围和使用说明，再完成最终收尾所需的候选核对、可共享复现材料及外部回归要求。pdf-evals 未执行，当前无需用户提供离线副本；本阶段没有提交、推送或创建 PR。
